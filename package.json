{
  "name": "twesix-homepage",
  "private": true,
  "scripts": {
    "dev":"cd src && parcel index.html",
    "build":"cd src && parcel build index.html --public-url ./ --out-dir ../build --no-cache --no-minify "
  },
  "dependencies": {
    "babel-preset-env": "^1.6.1",
    "parcel-bundler": "^1.5.1",
    "parcel-plugin-vue": "^1.5.0",
    "vue": "^2.5.13"
  }
}
